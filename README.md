# Registration stepper app on Angular


```sh
    yarn start
```

To create a production bundle:

```sh
    yarn build
```

Run tests:

```sh
    yarn test
```

Build Docker:

```sh
    yarn docker:build
```

Run Docker:

```sh
    yarn docker:run
```

